# Python packages needed to run Systems V1 (backend and launcher).
#
# Pinned to the exact versions in uv.lock, which is the source of truth;
# regenerate with:
#   uv export --format requirements-txt --no-dev --no-hashes --no-emit-project -o requirements.txt
#
# With uv (recommended) you need neither file: `uv run python run.py`
# installs everything from uv.lock. With pip, on Python 3.12 or newer:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)   or   source .venv/bin/activate
#   pip install -r requirements.txt
#   python run.py
#
# If `import pandas` fails on Windows with "An Application Control policy
# has blocked this file", Smart App Control is blocking pandas's compiled
# files for that Python version. Python 3.14 (what uv picks here) is known
# to work on the development machine.
#
# The frontend's packages are Node packages, listed in frontend/package.json;
# run.py installs them with npm on first start (Node.js 20 or newer).

annotated-doc==0.0.5
    # via fastapi
annotated-types==0.8.0
    # via pydantic
anyio==4.15.1
    # via
    #   starlette
    #   watchfiles
click==8.5.0
    # via uvicorn
fastapi==0.141.1
    # via systems-v1
h11==0.16.0
    # via uvicorn
httptools==0.8.0
    # via uvicorn
idna==3.20
    # via anyio
numpy==2.5.3
    # via pandas
pandas==3.0.6
    # via systems-v1
pydantic==2.13.5
    # via fastapi
pydantic-core==2.46.5
    # via pydantic
python-dateutil==2.9.0.post0
    # via pandas
python-dotenv==1.2.3
    # via uvicorn
pyyaml==6.0.3
    # via uvicorn
six==1.17.0
    # via python-dateutil
starlette==1.6.0
    # via fastapi
typing-extensions==4.16.0
    # via
    #   anyio
    #   fastapi
    #   pydantic
    #   pydantic-core
    #   starlette
    #   typing-inspection
typing-inspection==0.4.4
    # via
    #   fastapi
    #   pydantic
tzdata==2026.4 ; sys_platform == 'emscripten' or sys_platform == 'win32'
    # via pandas
uvicorn==0.53.0
    # via systems-v1
uvloop==0.22.1 ; platform_python_implementation != 'PyPy' and sys_platform != 'cygwin' and sys_platform != 'win32'
    # via uvicorn
watchfiles==1.3.0
    # via uvicorn
websockets==17.1
    # via uvicorn
