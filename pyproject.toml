[project]
name = "systems-v1"
version = "0.1.0"
description = "Systems V1 --- a deterministic simulation of a methodology-driven autonomous analytical system."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.uv]
package = false
